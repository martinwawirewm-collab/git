# Git Project Report

This project was done to practice Git from the beginning up to more advanced tasks such as branching, merging, rebasing, conflict resolution, cloning, working with remotes, and using a bare repository.

The work was first done locally, then pushed to remote repositories for review.

## Work directory

The project was organized inside a work directory:

```bash
~/Desktop/git
git/
├── hello/
├── cloned_hello/
└── hello.git

hello/ was the original repository
cloned_hello/ was the cloned repository
hello.git/ was the bare repository